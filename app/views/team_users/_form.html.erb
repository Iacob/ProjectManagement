<%= form_for(@team_user) do |f| %>
  <% if @team_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_user.errors.count, "error") %> prohibited this team_user from being saved:</h2>

      <ul>
      <% @team_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.text_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
